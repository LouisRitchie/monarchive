<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= if Map.has_key?(assigns, :title), do: assigns.title, else: "Depozz" %></title>
    <link rel="shortcut icon" href="<%= Routes.static_path(@conn, "/images/branding/favicon.png") %>" />
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <div class="layout-container">
      <div class="wordmark-container">
        <div class="monarchive-logo-container">
          <img src="<%= Routes.static_path(@conn, "/images/branding/logo.png") %>" alt="logo" />
        </div>
        <div class="monarchive-image-container">
          <img src="<%= Routes.static_path(@conn, "/images/branding/tree.png") %>" alt="tree" />
        </div>
      </div>
      <nav class="nav-container">
        <div class="leftside-links">
          <a href="/">Home</a>
          <a href="/subject/new">New Subject</a>
          <a href="/record/new">New Record</a>
        </div>
        <div class="rightside-links">
          <a href="/about">About</a>
        </div>
      </nav>
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>

      <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/layout/app.js") %>"></script>
    </div>
  </body>
</html>
